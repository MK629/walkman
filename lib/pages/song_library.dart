import 'package:flutter/material.dart';

class SongLibrary extends StatelessWidget {
  const SongLibrary({ super.key });

  @override
  Widget build(BuildContext context){
    return Text("Song library");
  }
}